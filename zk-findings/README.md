# zk-findings

`zk-findings/` is a local workspace for org findings during development and
testing.

Use this directory for non-public, engagement-specific finding artifacts that
should not be embedded into versioned plugin prompts.
